# @agent-delegate/core

Core schemas and decision logic for `agent-delegate`, an MCP-first delegation advisor for coding agents.

This package is intended to be used by the `agent-delegate` CLI and MCP server packages. For setup and usage, see the repository README:

https://github.com/HezLUO/agent-delegate
